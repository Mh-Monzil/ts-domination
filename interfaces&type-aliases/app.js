"use strict";
// Interfaces and Type Aliases
function interfaceFunc(obj) {
    console.log(obj.name);
}
interfaceFunc({
    name: "monzil",
    email: "monzil@gmail.com",
    password: "12345",
});
let c;
function valueFunc(obj) { }
valueFunc("12");
function checkIntersection(a) {
    // name, email, getDetails
}
