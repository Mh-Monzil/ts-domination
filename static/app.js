"use strict";
// don't need to create instance (new) to access data from class
class python {
    static getRandomNumber() {
        return Math.random();
    }
}
python.version = 1.0;
python.getRandomNumber();
